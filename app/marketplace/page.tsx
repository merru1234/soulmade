"use client";

import React from "react";

export default function MarketplacePage() {
  return (
    <div
      style={{
        minHeight: "100vh",
        background: "#f3f6fb",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: "system-ui, -apple-system, BlinkMacSystemFont, sans-serif",
      }}
    >
      <h1
        style={{
          fontSize: "40px",
          fontWeight: 700,
          color: "#123b8c",
        }}
      >
        TEST MARKETPLACE PAGE – MERWIN
      </h1>
    </div>
  );
}
